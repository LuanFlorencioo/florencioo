import Dialog from './Dialog';
import ProjectCard from './ProjectCard';
import Root from './Root';
import Title from './Title';

export const HomeProjects = Object.assign(Root, {
  Title,
  Dialog,
  ProjectCard,
})
